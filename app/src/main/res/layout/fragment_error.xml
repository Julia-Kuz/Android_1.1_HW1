<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".activity.ErrorFragment">

    <!-- TODO: Update blank fragment layout -->
    <ImageView
        android:id="@+id/imageError"
        android:layout_width="match_parent"
        android:layout_height="320dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf = "parent"/>

    <TextView
        android:id="@+id/errorConnection"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sorry :( \n Something went wrong"
        android:gravity="center"
        app:layout_constraintBottom_toTopOf="@id/tryAgain"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/imageError"/>

    <Button
        android:id="@+id/tryAgain"
        android:layout_marginTop="@dimen/common_spacing"
        android:text="Try again"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/errorConnection"
        app:layout_constraintEnd_toEndOf="@id/errorConnection"
        app:layout_constraintStart_toStartOf="@id/errorConnection" />

</androidx.constraintlayout.widget.ConstraintLayout>